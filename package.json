{
  "name": "podcast-mingli-world",
  "version": "1.0.0",
  "private": true,
  "description": "Daily Interview English — personal learning podcast",
  "scripts": {
    "deploy": "CLOUDFLARE_ACCOUNT_ID=0629de91516b2b363d1215708eaa5332 wrangler pages deploy site --project-name podcast-landing --branch main",
    "dev": "wrangler pages dev site",
    "test": "node --test tests/*.mjs && python3 -m unittest discover -s tests -p 'test_*.py'",
    "gates": "python3 -m scripts.lib.gates --manifest",
    "test:arrange": "python3 scripts/arrange.py",
    "test:curate": "python3 scripts/curate.py",
    "test:generate": "python3 scripts/generate.py",
    "test:publish": "python3 scripts/publish.py"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "puppeteer": "^25.3.0"
  }
}
